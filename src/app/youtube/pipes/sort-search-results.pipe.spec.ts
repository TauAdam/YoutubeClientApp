import { SortSearchResultsPipe } from './sort-search-results.pipe'

describe('SortSearchResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSearchResultsPipe()
    expect(pipe).toBeTruthy()
  })
})
